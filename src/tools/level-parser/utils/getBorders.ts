import type { Border } from '../../../../src/types/Border';

export default function getBorders(): Array<Border> {
  // get the border  coordinates
  // get the adjacent cells
  // break the cell into quadrants
  // determine which quadrants are borders
  // determine what kind of border to draw there as well?
  return [];
}
